# Closeout — 2026-09-17-admin-table-ux

- **Plan:** docs/pocket/plans/2026-09-17-admin-table-ux
- **Type:** phased
- **Started:** 2026-09-17  ·  **Closed:** 2026-09-18
- **Baseline SHA:** cb54e099350f5a0937cabcf239d3659e4b9abcbf  ·  **Final SHA:** c1b8053b6067d306e90c918b8b1a72617506054c
- **Result:** CLOSED — all phases DONE, all reviewable tasks REVIEW_PASS

## Phases

### Phase 1 — execution-plan/phase-1.md  (DONE)

| Task | Name | done_sha | Verdict |
|------|------|----------|---------|
| T1 | Backend `ListQuery` sort/total helper | f43fcdcf788c3d2799fc81f7da5fc1b50cd17d33 | REVIEW_PASS |
| T2 | Frontend `admin-table.ts` helper | 6a45425237b162fe219768b1a1d477b3922a2331 | REVIEW_PASS |
| T3 | `Table.tsx` — density + sortable header | 79824b673bfd914441c07b63a4571886e04dc2d2 | REVIEW_PASS |
| T5 | `AdminOutletController@index` — sort + total + summary + normalisasi meta | 4ee10f07e3d4e5ca11e3b8ce8a5de6eea5caf8db | REVIEW_PASS |
| T6 | `UserRoleController@index` — offset cursor + sort + total | d4e0e1e71e1fb43f3df8f1421d0f472d9ccd2ea1 | REVIEW_PASS |
| T7 | `PromotionController@index` — id-cursor → offset + sort + total + summary | 421996bb10d2d3115b15f842f7ae07ea12458af8 | REVIEW_PASS |

_SHA range: cb54e099350f5a0937cabcf239d3659e4b9abcbf..421996bb10d2d3115b15f842f7ae07ea12458af8_

### Phase 2 — execution-plan/phase-2.md  (DONE)

| Task | Name | done_sha | Verdict |
|------|------|----------|---------|
| T8 | `OrderController@index` — offset cursor + sort + total | 29b0fc28d6e5ccb6712507733860b27f71ab04a0 | REVIEW_PASS |
| T9 | `ProductController@index` — additive sort/cursor/total (public endpoint) | 46ad726a767b9d5f433edb3e266cd8d70e7133c0 | REVIEW_PASS |
| T10 | `SalesPerformanceController@adminPerformance` — id-cursor → offset + sort + total | af3c73564273021a6a37365e55434c65a85381b1 | REVIEW_PASS |

_SHA range: 421996bb10d2d3115b15f842f7ae07ea12458af8..af3c73564273021a6a37365e55434c65a85381b1_

### Phase 3 — execution-plan/phase-3.md  (DONE)

| Task | Name | done_sha | Verdict |
|------|------|----------|---------|
| T4 | Table controls (Pagination/Summary/Density toggle + hook) | cf919270de9cc8a7e6893f9126071b83c2bec880 | REVIEW_PASS |
| T11 | Outlets page — sort/paging/summary/timestamps/density | 532ab8e3596f8fd85689974dd14dc2cef2f2960a | REVIEW_PASS |
| T12 | Products page — sort/paging/summary/timestamps/density | 15f2f18be0e2ea6c7ad141ee83b231eada04b23a | REVIEW_PASS |

_SHA range: af3c73564273021a6a37365e55434c65a85381b1..15f2f18be0e2ea6c7ad141ee83b231eada04b23a_

### Phase 4 — execution-plan/phase-4.md  (DONE)

| Task | Name | done_sha | Verdict |
|------|------|----------|---------|
| T13 | Users page — sort/paging/summary/timestamps/density | 02cab70588021afe08db8eaf650d1a9ee35bd03b | REVIEW_PASS |
| T14 | Promotions page — sort/paging/summary/timestamps/density | f9bb608327f21f136c7071de124ab6882ba695e0 | REVIEW_PASS |
| T15 | Sales-performance page — sort/paging/summary/density | 86adb2dc3ff9274275f6ea3d63450a8cd69100c2 | REVIEW_PASS |
| T16 | Orders page — sort/paging/summary/timestamps/density | a2b8e8dabd966f4146982d59ab0616a0b3f769b0 | REVIEW_PASS |
| T17 | Integration — admin table contract across pages + dummy parity | c1b8053b6067d306e90c918b8b1a72617506054c | REVIEW_PASS |

_SHA range: 15f2f18be0e2ea6c7ad141ee83b231eada04b23a..c1b8053b6067d306e90c918b8b1a72617506054c_

## Carried Forward

Non-blocking observations from review — accepted at close, recorded for follow-up.

- **T1** (Minor): Case-insensitive order handling is implemented (normalizeOrder uses strtolower) but not directly asserted: tests exercise 'asc' and invalid 'DROP' only, with no 'ASC'/'Desc' input proving case normalization. Coverage gap, not a correctness defect. — apps/api/tests/Unit/ListQueryTest.php:41-52
- **T1** (Minor): offset(?int $offset, int $limit) leaves $limit unused. This is the spec-mandated signature and the docblock explains it as the call-site contract, so it is intentional; noted only as a minor smell that a future unused-parameter linter could flag. — apps/api/app/Support/ListQuery.php:52-55
- **T2** (Minor): normalizeSort falls back to the global DEFAULT_SORT_COLUMN 'created_at', which is NOT a member of the sales-performance allowlist ('name','id'). This is per the task spec ('column outside allowlist → created_at') and mirrors backend ListQuery's global default, so it is non-blocking, but the T16 sales-performance integrator must not route 'achievement' through normalizeSort (it sorts client-side) or the server would re-fallback to name ASC. — apps/web/src/lib/admin-table.ts:35
- **T3** (Minor): Additive keyboard path has zero test coverage: sortable <th> gains tabIndex={0} and an Enter/Space onKeyDown handler, but no test dispatches a key event, so the new behaviour is unverified. The DELIVERABLE only requires click-driven onSort, so this is an untested extra rather than a spec miss. — apps/web/src/components/ui/Table.tsx:57-61,75-77
- **T3** (Minor): Comment accuracy: 'default MUST stay byte-identical to the legacy markup' is not literally true. For a non-sortable <th> the new class template yields an extra trailing space (the `${sortable ? ... : ''}` interpolation) versus the legacy single-space string. Purely cosmetic — browsers/CSS collapse class whitespace and no rendered style changes — and the padding tokens themselves are identical. — apps/web/src/components/ui/Table.tsx:23-24,78
- **T5** (Minor): meta.summary still issues three separate COUNT queries (total, active, inactive) via buildListMeta on top of the page query. Carry-over from cycle 1 and explicitly accepted by the spec (summary is defined as a separate aggregate query with the same filters; 'additional aggregate cost' accepted), so non-blocking. A single grouped aggregate (group by is_active) could reduce round-trips. — apps/api/app/Http/Controllers/AdminOutletController.php:130-142
- **T5** (Minor): The in-scope test file AdminOutletTest.php is now 537 lines, nominally above the ~300-line file heuristic. Pre-existing: it was already 379 lines at prev_sha (already past the threshold before T5) and 499 at the cycle-1 HEAD, so T5 did not cross the threshold; every test method remains small (max 38 lines) and cohesive, and the repo convention tolerates large feature test suites (e.g. OrderTest.php 918, MeasurementTest.php 643). Non-blocking; no split recommended as part of this task. — apps/api/tests/Feature/AdminOutletTest.php:1-537
- **T6** (Minor): Test coverage gap: no feature test exercises a non-zero offset cursor for the users endpoint (no `?cursor=15` case). Offset pagination is verified only by code read + an independent SQL probe (`limit 21 offset 40`), not by a RoleManagementTest assertion. A test proving page 2 returns the next slice would close the gap. — apps/api/tests/Feature/RoleManagementTest.php:263-368
- **T6** (Minor): Test coverage gap: no test seeds a NULL created_at row to assert nulls-last ordering in both directions for the users endpoint. The behaviour is correct (inherited from ListQuery::rawOrder, which has its own unit tests and was probed to emit `(created_at IS NULL) ASC, created_at DESC, id DESC`), and T5 added a feature-level nulls-last test for outlets, so this is a coverage inconsistency rather than a defect. — apps/api/tests/Feature/RoleManagementTest.php:263-368
- **T6** (Minor): Thin per-controller plumbing (scalarQueryString, the resolveSort wrapper, and the buildListMeta wrapper) is duplicated between AdminOutletController (T5) and UserRoleController (T6). This is 2 occurrences across plan files (below the 3+ identical-logic heuristic), the heavy logic already lives in the shared ListQuery helper, and T5's accepted pattern is being replicated as intended. Watch item: if T7–T10 repeat this plumbing, consider extracting a small list-scaffolding trait/base. — apps/api/app/Http/Controllers/UserRoleController.php:93-141
- **T6** (Minor): Redundant double clone: `buildListMeta(clone $query)` passes a clone, and buildListMeta then does `(clone $query)->count()` again — the inner clone is unnecessary (the outer already isolates the caller's builder). Harmless/defensive. Also, `ListQuery::offset()` is not used here (the controller inlines `max((int) ..., 0)`), consistent with T5's accepted inline style. — apps/api/app/Http/Controllers/UserRoleController.php:66,136
- **T7** (Minor): Duplication threshold reached: `scalarQueryString(Request,string,string)` is now byte-identical (md5-verified) across 3 controllers of the same plan — AdminOutletController.php:117, UserRoleController.php:121, PromotionController.php:114 — and the `resolveSort` wrapper is identical across UserRoleController.php:93 and PromotionController.php:72. T7 added the 3rd copy, so the '3+ identical logic' heuristic is technically met. Classified Minor (not Important) because: (a) it is a 5-line defensive input guard (boilerplate), while the drift-prone logic is already centralized in ListQuery; (b) the plan's architecture deliberately uses per-controller wiring and allocates no consolidation task; (c) fixing it in T7 would require touching already-approved out-of-scope files (T5/T6), which T7's file scope forbids; (d) the T6 auditor set the precedent (2 occurrences = Minor, explicit watch item for T7-T10). Recommend a dedicated follow-up consolidation task (small trait/base or a static ListQuery::scalarString) if T8-T10 add further copies. — apps/api/app/Http/Controllers/PromotionController.php:114-119
- **T7** (Minor): In-scope feature test file PromotionTest.php is now 654 lines (was 437 at prev_sha), nominally above the ~300-line file heuristic. Pre-existing trajectory (already 437 > 300 before T7) and consistent with the repo convention of large feature suites (OrderTest 918, MeasurementTest 643); every test method is small and cohesive. Non-blocking; no split recommended as part of this task. — apps/api/tests/Feature/PromotionTest.php:1-654
- **T7** (Minor): Cross-task integration observation (NOT an in-scope defect): the frontend promotions api.ts still reads top-level `data.has_more` and `data.next_cursor` (apps/web/src/app/admin/promotions/api.ts:114), which this new contract no longer emits (they now live at `meta.has_more` / offset `meta.cursor`). Until the Promotions page task (T14) lands, the promotions page's hasMore/nextCursor will read as false/null. This is an expected, plan-sequenced transition (backend T7 precedes frontend T14; the packet's rollback note explicitly accepts the old next_cursor frontend as unused) and is out of T7's judged file scope, so it is recorded as an integration risk for T14 rather than a T7 issue. — apps/web/src/app/admin/promotions/api.ts:114
- **T8** (Minor): OrderController.php is now 314 lines (was 255 pre-T8), marginally above the ~300-line refactor heuristic. Downgraded to Minor (not Important) because: the threshold is approximate ('~300'), the excess is ~5%, index() itself is within its ~50-line budget, growth is a small cohesive additive change (2 focused helpers), and the task was explicitly additive-only forbidding broader restructuring. Flagging for future tasks that touch this controller. — apps/api/app/Http/Controllers/OrderController.php:1-314
- **T8** (Minor): Redundant double clone: buildListMeta() receives an already-cloned builder and clones it again (`(clone $query)->count()`), while the call site also passes `clone $query`. Harmless (count() does not mutate the builder) but the inner clone is superfluous. — apps/api/app/Http/Controllers/OrderController.php:100,142
- **T9** (Minor): Redundant ORDER BY term: ListQuery::rawOrder('id','asc') yields `(id IS NULL) ASC, id ASC, id DESC`. For the default sort the trailing `id DESC` tiebreaker is unreachable because `id` is the unique, non-null primary key, and even for other allowlist columns the tiebreak is redundant whenever the resolved column is `id`. Harmless (deterministic ordering, no correctness impact, id is never NULL) â€” the helper is shared/out-of-scope for T9 and already unit-tested, so no change requested. — apps/api/app/Support/ListQuery.php:47 (consumed at apps/api/app/Http/Controllers/ProductController.php:46)
- **T9** (Minor): Duplication debt: `scalarQueryString(Request,string,string)` is now byte-identical (md5-verified) across 5 controllers of this plan â€” AdminOutletController.php:117, OrderController.php:126, ProductController.php:115, PromotionController.php:114, UserRoleController.php:121. T9 added the 5th copy, so the '3+ identical logic' heuristic is nominally met. Classified Minor (not Important) for consistency with the T6 (2 copies) and T7 (3 copies) auditors, and because: (a) it is a 5-line defensive input-guard boilerplate, while the drift-prone logic is centralized in ListQuery; (b) the plan's architecture deliberately uses per-controller wiring; (c) fixing it inside T9 would require editing 4 already-approved out-of-scope controllers, which T9's file scope forbids. Recommend a dedicated consolidation follow-up (e.g. a small trait/base controller or ListQuery::scalarString) rather than a T9 fix. — apps/api/app/Http/Controllers/ProductController.php:115-120
- **T9** (Minor): Test coverage gap: no test exercises a NON-ZERO offset cursor or an explicit `limit` for this endpoint (no `?limit=â€¦&cursor=â€¦` case asserting the second page / has_more=true). Offset semantics and the limit clamp are verified here only by code read + an independent probe (limit=5&cursor=5 â†’ ids 6..10; limit=200â†’100; limit=0â†’1), not by a ProductTest assertion. The array test `?cursor[]=5` only proves scalar-safety, not positive-offset paging. Same gap the T6 auditor noted for the users endpoint; a page-2 test would close it. — apps/api/tests/Feature/ProductTest.php:116-205
- **T9** (Minor): PRE-EXISTING (not introduced by T9), out of the T9 scalar-safety contract: `?search[]=x` still 500s because `Product::scopeSearch(?string $search)` type-hints string and `$request->query('search')` is passed straight through (ProductController.php:70). Verified: passing an array raises `TypeError: scopeSearch(): Argument #2 must be of type ?string, array given`. This behaviour is identical to the parent (29b0fc2) and the T9 contract only lists sort/order/cursor as required scalar-safe params, so it is not a T9 defect â€” noted for completeness only. — apps/api/app/Http/Controllers/ProductController.php:70
- **T10** (Minor): Cross-file duplication of `scalarQueryString(Request,string,string)` is now byte-identical (md5-verified) across 6 controllers of this plan (AdminOutlet, Order, Product, Promotion, SalesPerformance, UserRole). The '3+ identical logic across files of the same plan' refactor heuristic is nominally met. Classified Minor (not Important) for consistency with the T6/T7/T8/T9 auditors and because fixing it would require editing 5 already-approved out-of-scope controllers â€” which the criteria's red-flag rule ('improvements touching files OUTSIDE the task scope â†’ REVERT') forbids for T10. The drift-prone logic (allowlist, order expression, meta) is centralized in ListQuery; only this 5-line defensive guard is duplicated. Recommend a dedicated consolidation follow-up (e.g. a trait/base controller or `ListQuery::scalarString()`), not a T10 fix. — apps/api/app/Http/Controllers/SalesPerformanceController.php:154-160
- **T10** (Minor): Valid-sort + invalid-order quirk: `ListQuery::resolveSort(['name','id'],'name','nonsense','name','asc')` returns ['name','desc'] (helper falls back to its own hardcoded 'desc' for a valid column with an unparseable order), not the endpoint's declared default 'asc'. Still HTTP 200 and still an allowlist column, so it satisfies the 'no 422/500' contract; only the direction differs from the endpoint default. Unspecified by the T10 packet (which only mandates `?sort=name&order=asc` â†’ asc and `?sort=achievement` â†’ 200 fallback). The behaviour lives in the shared/out-of-scope ListQuery helper, so no T10 change requested â€” flagged for awareness. — apps/api/app/Support/ListQuery.php:25 (consumed at apps/api/app/Http/Controllers/SalesPerformanceController.php:140)
- **T10** (Minor): Default tiebreaker direction: `ListQuery::rawOrder('name','asc')` emits `(name IS NULL) ASC, name ASC, id DESC`, i.e. id DESC as the deterministic tiebreaker, whereas the spec Implementation Notes describe the sales-performance DB default as `name ASC, id ASC`. Harmless (name collisions among sales users are possible but the ordering stays deterministic and id is a unique non-null PK), and the direction comes from the shared out-of-scope helper; the T10 packet's stated requirement is only 'default name ASC (caller default ("name","asc"))', which is satisfied. — apps/api/app/Http/Controllers/SalesPerformanceController.php:97
- **T10** (Minor): Unused import `use App\Models\SalesTarget;` (controller:5) â€” the controller references no `SalesTarget::` symbol (grep count 0). This is PRE-EXISTING (present at parent 46ad726:5) and NOT introduced by T10; noted for completeness only. — apps/api/app/Http/Controllers/SalesPerformanceController.php:5
- **T10** (Minor): Redundant `(int)` cast around `$request->integer('limit', 100)` (controller:83) â€” `Request::integer()` already returns int. Trivial; no behavioural impact. — apps/api/app/Http/Controllers/SalesPerformanceController.php:83
- **T10** (Minor): CROSS-TASK OBSERVATION (not a T10 defect): the current frontend `apps/web/src/app/admin/sales-performance/api.ts:127-128` still reads top-level `data.has_more` and `data.next_cursor`. Under the new shape these are `meta.has_more` and removed, so that consumer is stale until T15 (phase 4, depends on T10) updates the sales-performance page. T10's scope is backend-only and the spec explicitly defers the frontend to the page task, so no T10 change is required â€” flagged so the orchestrator/T15 owner is aware the contract moved. — apps/web/src/app/admin/sales-performance/api.ts:127-128
- **T11** (Minor): Legacy hasMore hint paragraph ('Ada outlet lebih lanjut — sesuaikan filter jika diperlukan.') is still rendered (page.tsx:176) in addition to the new TablePagination, so on a non-final page the orientation text is duplicated. Spec Rule 3 states the paging UI 'menggantikan teks hasMore'. Non-blocking redundancy; consider removing the legacy paragraph in a later pass. — apps/web/src/app/admin/outlets/page.tsx:176
- **T12** (Minor): page.test.tsx is 409 lines (four nested describe blocks). Test files are naturally long and this mirrors the already-approved outlets page.test.tsx (399 lines), so it is not treated as a blocking split; noted only as a maintainability observation. — apps/web/src/app/admin/products/page.test.tsx:1
- **T12** (Minor): Because loadProducts is a useCallback keyed on `search` and the mount effect depends on loadProducts, changing the search input triggers an immediate fetch (cursor=0) on each keystroke in addition to the explicit 'Cari' button, so there is no debounce and a request may fire per keystroke. This is the established pattern inherited from the outlets page (same shape) and does not violate the spec's reset rule, so it is non-blocking. — apps/web/src/app/admin/products/page.tsx:62
- **T13** (Minor): loadUsers is a useCallback keyed on [roleFilter, search] and the mount effect depends on loadUsers, so editing the search input or the role Select fires an immediate fetch (cursor=0) on each change in addition to the explicit 'Terapkan filter' click — a redundant request per keystroke. This is the established pattern inherited from the products/outlets pages and does not violate the spec's reset rule, so it is non-blocking. — apps/web/src/app/admin/users/page.tsx:43
- **T13** (Minor): page.test.tsx is 432 lines across five nested describe blocks. Test files are naturally long and this mirrors the already-approved products/outlets page.test.tsx, so it is not treated as a blocking split; noted only as a maintainability observation. — apps/web/src/app/admin/users/page.test.tsx:1
- **T14** (Minor): The sort-defaulting idiom `const sortCol = filters.sort || 'created_at'; const sortOrder = filters.order === 'asc' ? 'asc' : 'desc';` is repeated across the dummy branches of outlets/products/users/promotions api.ts (4 occurrences on this plan), while the shared `normalizeSort` helper (lib/admin-table.ts:44) already encapsulates it. This is a pre-existing cross-page pattern inherited from the already-approved T4/T12/T13 dummy branches (none of which were flagged for it), and refactoring only promotions would make it inconsistent while refactoring all four would touch out-of-scope files — so it is recorded as a non-blocking observation rather than a defect introduced by T14. — apps/web/src/app/admin/promotions/api.ts:70-71
- **T14** (Minor): page.tsx is 307 lines and the single AdminPromotionsPage component spans ~270 lines (page.tsx:38-307), marginally above the ~300-line soft threshold / 50-line function heuristic. The bulk is declarative JSX (the columns array + two Modal blocks) and the shape mirrors the approved products (239) and users (217) pages, so it is noted as a maintainability observation rather than a blocking split; the sibling pages were approved at similar sizes. — apps/web/src/app/admin/promotions/page.tsx:38
- **T14** (Minor): listDummyPromotions does not validate the requested sort against SORT_ALLOWLISTS.promotions, so an out-of-allowlist column (e.g. 'bogus') resolves to an all-null comparison and falls back to id-DESC rather than the backend's documented created_at-DESC fallback. The UI only ever emits allowlisted columns via sortableColumns, and the test confirms the behavior is deterministic and non-throwing, so the divergence is unreachable in practice — the same non-allowlisted dummy behavior exists in the approved users/products/outlets branches. — apps/web/src/app/admin/promotions/api.ts:70-73
- **T15** (Minor): The dummy achievement comparator uses `Number(b.achievement) - Number(a.achievement)` (api.ts:96) rather than the `parseMoney` idiom mandated by the QUALITY BAR. It is harmless here (dummy achievement is code-generated via `amount.toFixed(2)`, always a clean numeric string) and the line is pre-existing (byte-identical in the f9bb6083 parent), while the actual client-side display sort on real server data correctly uses `parseMoney` (page.tsx:118) — so the must-have is satisfied. Noted only for consistency with the shared comparator/parseMoney convention. — apps/web/src/app/admin/sales-performance/api.ts:96
- **T15** (Minor): page.tsx is 231 lines and the single `AdminSalesPerformancePage` component spans ~208 lines (page.tsx:23-231), above the ~50-line function soft heuristic. The bulk is declarative JSX (the `columns` array + filter card), and the shape mirrors the approved products (239) / users (217) pages, so it is recorded as a maintainability observation rather than a blocking split. — apps/web/src/app/admin/sales-performance/page.tsx:23
- **T15** (Minor): page.test.tsx is 433 lines across four nested describe blocks. Test files are naturally long and this mirrors the already-approved products (409) / users (432) / promotions page.test.tsx, so it is not treated as a blocking split. — apps/web/src/app/admin/sales-performance/page.test.tsx:1
- **T16** (Minor): page.tsx uses very dense single-line JSX — the whole return tree is one ~2.5k-char line (page.tsx:77), and `showOrder`/`approve` are likewise single-line (page.tsx:70-71). This mirrors the already-approved reference pages (products/users/promotions), so it is a readability observation rather than a defect introduced by T16; no logic is obscured. — apps/web/src/app/admin/orders/page.tsx:77
- **T16** (Minor): page.test.tsx is 379 lines across three nested describe blocks. Test files are naturally long and this mirrors the already-approved sibling suites (products 409 / users 432 / promotions 433 / sales-performance 433), so it is not treated as a blocking split — noted only as a maintainability observation. — apps/web/src/app/admin/orders/page.test.tsx:1
- **T17** (Minor): Redundant duplicated line: `localStorage.setItem('ddp_token', 't-token')` appears twice (lines 39 and 41) around `useDummyStore.getState().reset()`. Harmless but a leftover that can be collapsed to a single call after reset(). — apps/web/src/__tests__/admin-table-integration.test.tsx:39-41
- **T17** (Minor): Three `as any` casts when calling `compareRows` (lines 73, 97, 131), whereas the production loaders use the more type-honest `as unknown as Record<string, unknown>`. Style-only; the shared comparator signature accepts a generic record. — apps/web/src/__tests__/admin-table-integration.test.tsx:73,97,131
- **T17** (Minor): File is 419 lines, marginally above the ~300-line soft heuristic. This is a test file and matches the already-approved sibling suites (products 409, users 432, promotions 433, sales-performance 433, orders 379) which were all accepted as non-blocking, so it is recorded as a maintainability observation rather than a required split. — apps/web/src/__tests__/admin-table-integration.test.tsx:1
- **T17** (Minor): The 'invalid sort falls back to the default WITHOUT throwing' test only asserts the returned row count is > 0; it does not additionally assert the fallback ORDER equals the default created_at DESC ordering. The test is non-vacuous (it proves no throw and non-empty output across all five loaders) and the default-order contract is separately covered by the per-loader parity tests, so this is a coverage-strength observation only. — apps/web/src/__tests__/admin-table-integration.test.tsx:167-188
- **T17** (Minor): The URL query assertion idiom (`expect(url).toContain('sort=…')` / `order` / `cursor` / `limit`) is repeated across the five Group-2 loader tests (25 assertions). It reads clearly and each targets a distinct loader/param, so extracting a shared helper is optional; noted for conciseness only. — apps/web/src/__tests__/admin-table-integration.test.tsx:234-380

_44 Minor observations carried forward (0 Critical / 0 Important). Phase-level pass findings: phase-1 CLEAN, phase-2 RESOLVED (2 Minor, non-blocking), phase-3 CLEAN, phase-4 CLEAN (1 Minor, non-blocking)._

## Skipped Tasks

_None — all 17 tasks were DONE with a non-null done_sha and a current REVIEW_PASS verdict._
